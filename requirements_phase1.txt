# Phase 1 Requirements
# Install in this order to avoid conflicts

# Core deep learning
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0

# CLIP — must be installed from GitHub (not on PyPI)
# Run: pip install git+https://github.com/openai/CLIP.git

# Utilities
Pillow>=9.0.0
tqdm>=4.65.0
numpy>=1.24.0
ftfy>=6.1.1
regex>=2023.0.0

# Web framework
streamlit>=1.30.0

# OWL-ViT object detection (much more accurate than CLIP zero-shot)
transformers>=4.35.0
